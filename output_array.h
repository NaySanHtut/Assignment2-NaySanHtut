#ifndef OUTPUT_ARRAY_H
#define OUTPUT_ARRAY_H // idk redefine in case of being compiled more than once? It was taught in lectures so I included it...

void outputArray(double *arr, int size);

#endif
